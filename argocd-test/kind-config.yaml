kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: argocd-test
nodes:
  - role: control-plane
    extraPortMappings:
      # ArgoCD UI를 localhost:8080 으로 노출 (선택 사항, port-forward 대신 사용 가능)
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
